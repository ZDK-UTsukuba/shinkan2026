---
import { GoogleFontsOptimizer } from "astro-google-fonts-optimizer";
import BaseHead, { type Props as HeadProps } from "../components/BaseHead.astro";
import Header from "../components/Header.astro";

import "../styles/reset.css";
import "../styles/global.css";

export type Props = HeadProps;
const { ...head } = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <BaseHead {...head} />
    <GoogleFontsOptimizer
      url="https://fonts.googleapis.com/css2?family=Inter:wght@200;400;500;700&family=Noto+Sans+JP:wght@400;700&display=swap"
    />
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-JGRZYF891V" is:inline data-astro-rerun></script>
    <script is:inline data-astro-rerun>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());
      gtag("config", "G-JGRZYF891V");
    </script>
  </head>
  <body>
    <div class="notice-bar">
      本サイトの掲載情報は2026年5月時点のものであり、一部情報が古くなっている可能性があります。最新の情報につきましては2026年度版新歓Webの公開をお待ちください。
    </div>
    <div class="root container">
      <Header />
      <main>
        <slot />
      </main>
    </div>
    <style>
      .notice-bar {
        background-color: #fff3cd;
        color: #856404;
        text-align: center;
        padding: 0.75rem;
        width: 100%;
        margin-bottom: 1rem;
        font-size: 0.9rem;
      }
      .root {
        display: flex;
        flex-direction: row-reverse;
        width: 100%;
        min-height: 100dvh;
        padding-block: 3.75rem;
        gap: min(3rem, 3vw);
      }
      main {
        flex: 1;
      }
      @media screen and (max-width: 670px),
        screen and (max-height: 615px),
        screen and (max-width: 1250px) and (max-height: 552px) {
        .root {
          flex-direction: column;
          padding-block: 0 3.75rem;
          gap: 0.5rem;
          max-width: initial;
          padding-inline: 0;
          margin-inline: 0;
        }
      }
    </style>
  </body>
</html>
